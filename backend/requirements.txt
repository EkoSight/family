fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.6
# Optional production drivers / LLM backends (uncomment as needed):
# psycopg[binary]>=3.1        # PostgreSQL driver
# anthropic>=0.40             # LLM_PROVIDER=claude
# google-generativeai>=0.8    # LLM_PROVIDER=gemini
